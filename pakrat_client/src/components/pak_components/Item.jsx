export default function Item ({itemInfo, itemIndex}) {
    console.log(itemInfo)
    return (
        <div>
            <div>{itemInfo.name}</div>
            <div>
                
            </div>
        </div>
    )
}